title: notmutt
description: An async mail client built on notmuch
remote_theme: just-the-docs/just-the-docs@v0.10.1

# the repo link in the top-right of every page (just-the-docs aux_links)
aux_links:
  "View on GitHub":
    - "https://github.com/fishman/notmutt"
aux_links_new_tab: true

# agent-working docs (plans, specs, benchmarks): Go literals in code
# blocks contain {{ which Liquid parses as template tags - not site content
exclude:
  - superpowers
