title: notmutt
description: An async, command-line-first mail client built on notmuch
remote_theme: pages-themes/cayman@v0.2.0
show_downloads: false

# agent-working docs (plans, specs, benchmarks): Go literals in code
# blocks contain {{ which Liquid parses as template tags - not site content
exclude:
  - superpowers
